body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f7f4f0; /* Light Beige */
    color: #333;
}

header {
    padding: 20px;
    text-align: left;
    display: flex;
    align-items: center; /* Выравниваем по вертикали внутри header */
    justify-content: space-between;
    background-color: #2c3e50;
    color: white;
    height: 80px; /* Задаем высоту для header (примерно) */
}

header a {
    text-decoration: none;
    color:  #46b1ae;
}

header h1 {
    margin: 0;
    font-size: 2em;
    line-height: 1; /* Убираем межстрочный интервал, чтобы не влиял на высоту */
}

/* Top Menu */
.top-menu {
    margin-right: 20px;
    height: 100%; /* Растягиваем меню на всю высоту header */
    display: flex; /* Используем flexbox для выравнивания пунктов меню */
    align-items: center; /* Центрируем пункты меню по вертикали */
}

.top-menu ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    align-items: center;
    height: 100%; /* Растягиваем список на всю высоту меню */
}

.top-menu li {
    margin-left: 20px;
    height: 100%; /* Растягиваем пункты списка на всю высоту меню */
    display: flex; /* Используем flexbox для выравнивания ссылок */
    align-items: center; /* Центрируем ссылки по вертикали */
}

.top-menu li:first-child {
    margin-left: 0;
}

.top-menu a {
    text-decoration: none;
    color: #46b1ae;
    font-size: 1.1em;
    font-weight: bold;
    transition: color 0.3s ease, transform 0.3s ease; /* Плавное изменение цвета и масштаба */
    display: inline-block; /* Важно для применения transform */
}

.top-menu a:hover {
    color: white;
    transform: scale(1.05); /* Увеличение масштаба на 10% */
}

/* Slider */
.slider {
    width: 100%;
    overflow: hidden;
    position: relative;
    margin-top: 60px;
}

.slider-images {
    display: flex; /* Выстраиваем изображения в ряд */
    transition: transform 0.3s ease; /* Добавим плавный переход */
    justify-content: center;
    align-items: center; /* Центрируем изображения по вертикали (если нужно) */
    gap: 20px; /* Добавляем отступ между изображениями */
    padding-left: 1000px; /* Добавляем отступ слева, чтобы первое фото было видно */
}

.slider-images img {
    width: 100%; /* Занимают всю доступную ширину */
    max-width: 1200px; /* Ограничиваем максимальную ширину */
    height: auto; /* Высота подстраивается автоматически */
    object-fit: cover; /* Масштабируем изображение с обрезкой, чтобы заполнить контейнер */
    flex-shrink: 0; /* Запрещаем сжиматься */
    /* margin: 0 auto;  Удаляем, так как используем gap и padding */
}

.slider-controls {
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    transform: translateY(-50%);
    padding: 0 20px;
}

.slider-controls button {
    background-color: rgba(0, 0, 0, 0.5);
    color: white;
    border: none;
    padding: 10px 15px;
    cursor: pointer;
    font-size: 1.2em;
}

/* Location */
.location {
    padding: 50px;
    text-align: center;
}

.location h2 {
    font-size: 2em;
    margin-bottom: 10px;
}

.underline {
    width: 50px;
    height: 3px;
    background-color: #46b1ae; /* Turquoise */
    margin: 0 auto 20px;
}

.location-content {
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: 20px;
}

.location-content .text-content {
    width: 45%;
    text-align: left;
    font-size: 1.1em;
    line-height: 1.6;
}

.location-content .image-content {
    width: 45%;
}

.location-content img {
    width: 100%;
    border-radius: 5px;
}

/* Footer */
footer {
    background-color: #000000; /* Light Beige */
    padding: 30px;
    text-align: center;
    font-size: 0.9em;
    color: #ffffff;
}

footer img {
    vertical-align: middle;
    margin-right: 5px;
    width: 20px;
}

/* Media Queries for Responsiveness */
@media (max-width: 768px) {
    .location-content {
        flex-direction: column;
    }

    .location-content .text-content,
    .location-content .image-content {
        width: 90%;
        margin-bottom: 20px;
    }

    .slider-images img {
        width: 90%; /* Увеличиваем ширину изображений на маленьких экранах */
    }
}